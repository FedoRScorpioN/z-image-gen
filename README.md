# Z-Image Generator

**Локальный генератор изображений на базе Z-Image-Turbo, оптимизированный для 4GB VRAM**

Это приложение позволяет генерировать изображения по текстовому описанию (промпту) локально на вашем компьютере, даже если у вас всего 4GB видеопамяти (например, NVIDIA GeForce RTX 3050).

## Возможности

- 🎨 **Генерация изображений 768x512** по текстовому описанию
- 💾 **Оптимизация для 4GB VRAM** - работает на видеокартах с ограниченной памятью
- 🚀 **Простая установка** - одна команда и готово
- 📥 **Автозагрузка модели** - при первом запуске автоматически скачивается модель
- 💻 **CLI интерфейс** - работает из командной строки
- 🔄 **Интерактивный режим** - для генерации множества изображений

## Требования

- **Python 3.10+**
- **NVIDIA GPU** с CUDA поддержкой (4GB+ VRAM)
- **CUDA Toolkit** (рекомендуется 12.x)
- **~4GB свободного места** на диске для модели

## Установка

### Вариант 1: Из GitHub (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/FedoRScorpioN/z-image-gen.git
cd z-image-gen

# Установите зависимости
pip install -e .
```

### Вариант 2: Прямая установка

```bash
pip install git+https://github.com/FedoRScorpioN/z-image-gen.git
```

## Использование

### Быстрый старт

```bash
# Генерация одного изображения
z-image-gen "красивый закат над горами"

# С указанием параметров
z-image-gen "киберпанк город ночью" --width 1024 --height 576

# С фиксированным seed для воспроизводимости
z-image-gen "кот в космосе" --seed 42
```

### Интерактивный режим

```bash
z-image-gen --interactive
```

В интерактивном режиме вы можете вводить промпты один за другим без перезагрузки модели.

### Параметры командной строки

| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| `prompt` | - | Текстовое описание изображения |
| `-w, --width` | 768 | Ширина изображения |
| `-H, --height` | 512 | Высота изображения |
| `-s, --steps` | 4 | Количество шагов генерации |
| `--seed` | -1 | Seed (-1 для случайного) |
| `-m, --model` | q4_0 | Тип модели (q4_0, q5_0, q8_0) |
| `-o, --output` | автто | Путь для сохранения |
| `-i, --interactive` | - | Интерактивный режим |

### Полезные команды

```bash
# Предварительная загрузка модели
z-image-gen --download-model

# Информация о системе
z-image-gen --info

# Список доступных моделей
z-image-gen --list-models

# Версия
z-image-gen --version
```

## Модели

Доступны три варианта квантизации модели Z-Image-Turbo:

| Модель | Размер | Требования VRAM | Рекомендуется |
|--------|--------|-----------------|---------------|
| Q4_0 | 3.68 GB | 4GB | ✅ Для RTX 3050 и подобных |
| Q5_0 | 4.54 GB | 6GB | Для 6GB VRAM |
| Q8_0 | 6.58 GB | 8GB+ | Для 8GB+ VRAM |

По умолчанию используется **Q4_0** - оптимальный выбор для 4GB VRAM.

## Примеры промптов

```
# Пейзажи
"красивый закат над горами, цифровое искусство"
"туманный лес с лучами солнца, фэнтези стиль"

# Портреты
"портрет девушки с голубыми глазами, кинематографичный стиль"
"старый мудрец с длинной бородой, фэнтези арт"

# Sci-Fi / Киберпанк
"киберпанк город ночью, неоновые огни, дождь"
"космический корабль в открытом космосе, детализированный"

# Фэнтези
"дракон летит над замком, эпичная сцена"
"магический лес с светящимися грибами"
```

## Решение проблем

### "CUDA not available"

Убедитесь, что:
1. Установлен NVIDIA драйвер последней версии
2. Установлен CUDA Toolkit
3. PyTorch установлен с CUDA поддержкой

### "Insufficient VRAM"

Если возникает ошибка нехватки видеопамяти:
1. Используйте модель Q4_0 (наименее требовательная)
2. Уменьшите разрешение: `--width 512 --height 288`
3. Закройте другие приложения, использующие GPU

### Медленная генерация

Первая генерация может быть медленной из-за:
- Загрузки модели в память
- Компиляции CUDA ядер

Последующие генерации будут быстрее.

## Технические детали

- **Backend**: stable-diffusion.cpp (C++ реализация)
- **Python wrapper**: stable-diffusion-cpp-python
- **Model**: Z-Image-Turbo (квантованная GGUF версия)
- **Оптимизации для 4GB VRAM**:
  - Offload параметров в RAM
  - Flash Attention
  - VAE на CPU
  - Tiled VAE декодирование

## Лицензия

MIT License

## Благодарности

- [Tongyi-MAI](https://github.com/Tongyi-MAI/Z-Image) - создатели Z-Image модели
- [leejet](https://github.com/leejet/stable-diffusion.cpp) - stable-diffusion.cpp реализация
- [HuggingFace](https://huggingface.co/leejet/Z-Image-Turbo-GGUF) - хостинг GGUF моделей
